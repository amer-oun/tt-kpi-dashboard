# Librairies necessaires pour LANCER LE DASHBOARD (app.py).
# C'est ce fichier que Streamlit Community Cloud utilise au deploiement.
# Le dashboard lit seulement les CSV : il n'a pas besoin de Prophet/scipy.
# (versions volontairement souples pour s'installer sur n'importe quelle
#  version recente de Python en ligne)
# >=1.50 : la syntaxe width="stretch" (qui remplace use_container_width,
# supprime fin 2025) doit etre disponible.
streamlit>=1.50
pandas>=2.2
plotly>=5.22
numpy>=1.26
fpdf2>=2.7

# Lecture des fichiers Excel deposes dans l'onglet "Saisie / Import"
# (pd.read_excel). Sans elles, l'import .xlsx/.xls echoue en ligne :
#   openpyxl -> fichiers .xlsx (format moderne)
#   xlrd     -> fichiers .xls  (ancien format Excel)
openpyxl>=3.1
xlrd>=2.0
